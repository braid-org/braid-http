This is a demo blog / chat.

To run the demo:

    git clone https://github.com/braid-org/braid-http.git
    cd braid-http/demos/blog
    npm install
    node server.js

Then open https://localhost:3008/ in your browser, and then click through the
self-signed certificate warnings to "proceed anyway".